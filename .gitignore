node_modules/
dist/
build/
.env
.env.local
.claude/settings.local.json
*.mp4
*.mov
*.log
.DS_Store
Thumbs.db

# Generated by docker/webgateway/generate-csp-ini.sh from CSP.ini.example on first
# `compose up`. It carries the obfuscated CSPSystem password, so it is a credential file
# even though the default value is a well-known one -- root CLAUDE.md §5. The template is
# committed; the rendered file never is.
docker/webgateway/CSP.ini
